# Generated by precompute.py — regenerable, can be large, never committed (spec: no artifact >100MB)
cache/
# isolated caches for local test datasets (synthetic/dataset_b/dataset_c)
cache_*/

# Local data (the official 487MB candidates.jsonl is not ours to redistribute / too large for git)
*.jsonl
*.jsonl.gz

# Submission outputs
submission*.csv
# local test-set outputs (dataset_b_submission.csv, etc.)
*_submission.csv
!sample_submission.csv

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# Downloaded embedding model (tested and NOT adopted — dense embeddings hurt on this task; see notebooks/llm_judge_validation.md)
models/
