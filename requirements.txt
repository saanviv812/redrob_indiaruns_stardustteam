# Redrob Ranker — pinned dependencies (CPU-only, no GPU/network at rank time).
# Stage 3 reproduces rank.py inside a sandbox from this file, so versions are pinned.
# Deliberately minimal: no transformers/torch (see UPDATED_ARCHITECTURE.md §1.1) — the
# only heavy dep is scikit-learn, which we already need for TF-IDF + TruncatedSVD.
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
rank-bm25==0.2.2
PyYAML==6.0.2
